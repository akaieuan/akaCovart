# Hoist framework-shared deps so Next.js can resolve React/three from app node_modules
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
# Allow workspace packages to be linked directly
link-workspace-packages=true
